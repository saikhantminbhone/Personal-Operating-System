name = "personal-os-marketing"
compatibility_date = "2024-09-23"

# Workers + Static Assets: no worker script, just serve the static export
# from Next.js (`out/`) as assets. `wrangler deploy` uploads the folder.
[assets]
directory = "./out"
not_found_handling = "404-page"
